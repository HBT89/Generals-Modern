#pragma once
#ifndef SRVECTOR4_HPP
#define SRVECTOR4_HPP

// Surrender Engine stub - proprietary library removed
// Minimal srVector4 implementation for compilation

class srVector4
{
public:
    float x, y, z, w;

    srVector4() : x(0.0f), y(0.0f), z(0.0f), w(0.0f) {}
    srVector4(float _x, float _y, float _z, float _w) : x(_x), y(_y), z(_z), w(_w) {}
};

#endif // SRVECTOR4_HPP
