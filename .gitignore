/Run
/Profile
/Tools
/Backup
/.obsidian
/.vscode
/.git
/GeneralsMD/code/build/Release/Data
# Generated shader headers
*.vsh_code.h
*.psh_code.h

# Visual C++ files
*.ncb
*.opt
*.plg

# Game Assets
*.big
*.bik

# Build output (CMake/MSBuild) — generated, never commit
/build/
/Testing/

# Runtime debug/trace logs
/bgfx_*.log
/bgfx_*.log.*.bak

# Local capture screenshots (verification harness output)
/tools/shots/

# Local Claude Code settings
.claude/settings.local.json

# Scratch build-log dumps and file backups
/build_*.txt
w3dview_original.cpp

# Local CMake toolchain. A full CMake 3.31.4 Windows distribution (~8,000 files,
# including cmake-gui.exe) used to be committed here, which is not ours to
# redistribute and dominated the repository size. It stays on disk because the
# documented build command invokes it directly, but it is no longer tracked.
# Building from a fresh clone requires CMake >= 3.20 installed separately.
/cmake/